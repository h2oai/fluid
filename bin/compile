#!/usr/bin/env node
// Generated by CoffeeScript 1.9.3
(function() {
  var _, app_coffee, app_js, argv, deploy, dist_dir, fluid, fs, fse, html, index_html, output_dir, path, recompile, ref, ref1, script, scriptTags, scripts, stylesheet, stylesheetTags, stylesheets;

  argv = require('minimist')(process.argv.slice(2));

  path = require('path');

  fs = require('fs');

  fse = require('fs-extra');

  fluid = require('../fluid.js');

  _ = require('lodash');

  if (argv.compile) {
    app_coffee = _.isArray(argv.compile) ? argv.compile[0] : argv.compile;
  } else {
    console.log("Usage: fluid [options] --compile app.coffee\n\nOptions:\n  --compile      compile to HTML, JavaScript and CSS\n  --help         display this help message\n  --include-css  include a CSS file in index.html\n  --include-js   include a JavaScript file in index.html\n  --watch        watch for changes and recompile\n\nExamples:\n  fluid --compile app.coffee\n  fluid --compile app.coffee --js foo.js\n  fluid --compile app.coffee --js foo.js --js bar.js\n  fluid --compile app.coffee --js foo.js --js bar.js --css baz.css\n  fluid --compile app.coffee --js foo.js --js bar.js --css baz.css --watch");
    process.exit(1);
  }

  if (argv.verbose) {
    console.log('Using opts:');
    console.dir(argv);
  }

  output_dir = path.dirname(app_coffee);

  app_js = path.join(output_dir, path.basename(app_coffee, path.extname(app_coffee)) + '.js');

  dist_dir = path.join(__dirname, '..', 'dist');

  deploy = function(name) {
    var file_name;
    file_name = path.join(output_dir, name);
    console.log("Writing " + file_name + ".");
    return fse.copySync(path.join(dist_dir, name), file_name);
  };

  deploy('fluid.css');

  deploy('fluid.js');

  scripts = (ref = argv['include-js']) != null ? ref : [];

  if (!_.isArray(scripts)) {
    scripts = [scripts];
  }

  scriptTags = (function() {
    var i, len, results;
    results = [];
    for (i = 0, len = scripts.length; i < len; i++) {
      script = scripts[i];
      results.push("<script src=\"" + script + "\"></script>");
    }
    return results;
  })();

  stylesheets = (ref1 = argv['include-css']) != null ? ref1 : [];

  if (!_.isArray(stylesheets)) {
    stylesheets = [stylesheets];
  }

  stylesheetTags = (function() {
    var i, len, results;
    results = [];
    for (i = 0, len = stylesheets.length; i < len; i++) {
      stylesheet = stylesheets[i];
      results.push("<link href=\"" + stylesheet + "\" rel=\"stylesheet\" type=\"text/css\">");
    }
    return results;
  })();

  html = fse.readFileSync(path.join(dist_dir, 'index.html'), {
    encoding: 'utf8'
  });

  html = html.replace('<!--fluid_styles-->', stylesheetTags.join(''));

  html = html.replace('<!--fluid_scripts-->', scriptTags.join(''));

  index_html = path.join(output_dir, 'index.html');

  console.log("Writing " + index_html + ".");

  fse.writeFileSync(index_html, html, {
    encoding: 'utf8'
  });

  recompile = function() {
    var js;
    js = fluid._compile(fse.readFileSync(app_coffee, {
      encoding: 'utf8'
    }));
    console.log("Writing " + app_js + ".");
    return fse.writeFileSync(app_js, js, {
      encoding: 'utf8'
    });
  };

  recompile();

  if (argv.watch) {
    console.log("Watching " + app_coffee + "...");
    fs.watchFile(app_coffee, recompile);
  }

}).call(this);
